import React from 'react'
import { Popover, PopoverContent, PopoverTrigger } from './ui/popover'

// Create component of change members

const ChangeMembersPopoverTemplate = () => {
  return (
    <div>
        <Popover>
            <PopoverTrigger>Change Members</PopoverTrigger>
            <PopoverContent>

            </PopoverContent>
        </Popover>
    </div>
  )
}

export default ChangeMembersPopoverTemplate